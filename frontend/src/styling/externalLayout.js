import styled from 'styled-components';

/* =======================================================================
All styling which affects the layout of an element relative to its parent
======================================================================= */

export const BorderBox = styled.div`
    box-sizing: border-box;
`;